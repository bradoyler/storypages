  <p>Title: {{title}}</p>

  {{#each body}}
    <{{type}}> {{{html}}} </{{type}}>
  {{/each}}


  <h4>Authors:</h4>
  <ul>
      {{#each authors}}
        <li>{{name}}</li>
      {{/each}}
  </ul>
